const routes = {
  home: '/',
  login: '/login',
  buildings: '/buildings',
  tasks: '/tasks',
  dashboard: '/dashboard',
  apartments: '/apartments',
  messages: '/messages'
};

export default routes;
